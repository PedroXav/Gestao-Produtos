<Window x:Class="Desktop.UsuarioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cadastro de Usuário" Height="300" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nome -->
        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,5">
            <TextBlock Text="Nome:" FontWeight="Bold" Margin="0,0,0,2"/>
            <TextBox x:Name="NomeBox" Height="30" Padding="5" />
        </StackPanel>

        <!-- Senha -->
        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,5">
            <TextBlock Text="Senha:" FontWeight="Bold" Margin="0,0,0,2"/>
            <PasswordBox x:Name="SenhaBox" Height="30" Padding="5"/>
        </StackPanel>

        <!-- Ativo -->
        <CheckBox x:Name="AtivoBox" Content="Usuário Ativo" Grid.Row="2" Margin="0,10,0,0" FontWeight="Bold"/>

        <!-- Botões -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Salvar" Width="100" Height="35" Margin="5"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"
                    Click="Salvar_Click"/>
            <Button Content="Cancelar" Width="100" Height="35" Margin="5"
                    Background="#F44336" Foreground="White" FontWeight="Bold"
                    Click="Cancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
